# HabitMax Telegram Bot 🤖

Мигрированный бот для формирования привычек с платформы Max Messenger на Telegram с AI-интеграцией через OpenRouter.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте (Windows)
venv\Scripts\activate

# Активируйте (Linux/Mac)
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл
# Обязательно укажите:
# - BOT_TOKEN (получить у @BotFather)
# - OPENROUTER_API_KEY (получить на https://openrouter.ai/keys)
```

### 3. Запуск

```bash
python main.py
```

## 📋 Функционал

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота, главное меню |
| `/add_habit` | Добавить новую привычку (с FSM диалогом) |
| `/my_habits` | Список активных привычек |
| `/my_progress` | Статистика и прогресс |
| `/ai_advice` | AI-рекомендация по привычкам |
| `/analyze_patterns` | Анализ паттернов пользователя |
| `/settings` | Настройки бота |
| `/help` | Помощь |

### AI-функции 🤖

1. **Персонализированные рекомендации**
   - Анализ истории выполнения
   - Контекстуальные советы на основе успехов/неудач
   - Оптимизация токенов (не отправляется вся история)

2. **Умные напоминания**
   - Учитывают день недели и время суток
   - Персонализированный стиль (friendly/strict/motivational)
   - Привязка к прогрессу серии

3. **Анализ паттернов**
   - Определение самого продуктивного времени
   - Выявление проблемных привычек
   - Долгосрочное хранение метрик в БД

## 🏗 Архитектура

```
habitmax_telegram_bot/
├── app/
│   ├── models/           # SQLAlchemy модели
│   │   ├── base.py       # Базовая модель
│   │   ├── user.py       # Пользователь
│   │   ├── habit.py      # Привычка + логи
│   │   └── ai_context.py # AI-контекст
│   ├── services/         # Бизнес-логика
│   │   ├── database.py   # Repository pattern
│   │   ├── ai_service.py # OpenRouter интеграция
│   │   └── reminder_service.py # Планировщик
│   ├── handlers/         # Aiogram хендлеры
│   │   ├── common.py     # Общие команды
│   │   ├── habits.py     # CRUD привычек + FSM
│   │   └── ai_handlers.py # AI-команды
│   ├── middlewares/      # Middleware
│   │   └── services.py   # DI middleware
│   ├── utils/            # Утилиты
│   │   └── logger.py     # Логирование
│   ├── config.py         # Pydantic Settings
│   └── __init__.py
├── migrations/           # Alembic миграции
├── tests/                # Тесты
├── main.py               # Точка входа
├── requirements.txt      # Зависимости
├── .env.example          # Пример конфигурации
└── README.md             # Этот файл
```

## 🗄 Модели данных

### User
- `id` - Telegram ID
- `username`, `first_name`, `last_name` - Профиль
- `timezone` - Часовой пояс
- `notification_enabled`, `ai_enabled` - Настройки
- `streak_days`, `total_completions` - Статистика

### Habit
- `id`, `user_id` - Идентификаторы
- `name`, `description`, `emoji` - Описание
- `reminder_time`, `frequency` - Напоминания
- `target_days`, `current_streak`, `best_streak` - Прогресс
- `is_active` - Статус

### HabitLog
- `habit_id`, `user_id` - Ссылки
- `completed_date`, `status` - Выполнение
- `notes`, `mood` - Дополнительно

### AIContext
- `user_id` - Ссылка на пользователя
- `most_productive_day/time` - Паттерны
- `struggling_habits` - Проблемные зоны
- `successful_patterns` - Что работает
- `preferred_reminder_style` - Предпочтения

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | - |
| `OPENROUTER_API_KEY` | API ключ OpenRouter | - |
| `OPENROUTER_MODEL` | Модель AI | `meta-llama/llama-3.1-8b-instruct` |
| `DATABASE_URL` | URL базы данных | `sqlite+aiosqlite:///./habitmax.db` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `ADMIN_IDS` | ID администраторов | - |

### Бесплатные модели OpenRouter

- `meta-llama/llama-3.1-8b-instruct` (рекомендуется)
- `mistralai/mistral-7b-instruct`
- `google/gemma-2-9b-it`

## 🔄 Миграция с Max Messenger

### Что было перенесено:
- ✅ CRUD привычек
- ✅ Логирование выполнения
- ✅ Статистика и серии
- ✅ Напоминания
- ✅ Профиль пользователя

### Что добавлено:
- 🤖 AI-рекомендации через OpenRouter
- 📊 Анализ паттернов
- 💬 Интерактивные диалоги (FSM)
- 🔔 Умные напоминания

### Технические изменения:
- Node.js → Python 3.10+
- Express → Aiogram 3.x
- PostgreSQL → SQLAlchemy 2.0 (SQLite/PostgreSQL)
- Redis+BullMQ → APScheduler
- Max Platform API → Telegram Bot API

## 🧪 Разработка

### Запуск линтеров

```bash
black app/
ruff check app/
```

### Создание миграции

```bash
alembic revision --autogenerate -m "description"
alembic upgrade head
```

## 📝 Лицензия

MIT © 2026 HabitMax

## 🤝 Контрибьюция

1. Fork репозитория
2. Создайте feature branch
3. Commit изменения
4. Push и создайте Pull Request

---

Сделано с ❤️ на Python
