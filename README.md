# HabitMax Bot 🚀

Telegram-бот для трекинга привычек с AI-рекомендациями и финансовыми напоминаниями.

## Возможности

### 🎯 Привычки
- Добавление и отслеживание привычек
- Ежедневные напоминания
- Подсчёт серий (streak)
- Система достижений
- Быстрое выполнение (< 200мс)

### 💰 Финансы
- Напоминания о платежах и вкладах
- Поддержка банков: Т-Банк, Сбербанк, ВТБ, Альфа-Банк, Тинькофф
- Напоминания за 3 дня, 1 день и в день платежа
- Отметка выполнения прямо в чате

### 🤖 AI (в разработке)
- Персонализированные рекомендации
- Анализ прогресса
- Статистика и прогнозы

## Установка

```bash
# Клонировать репозиторий
git clone https://github.com/timoshinoleg-eng/habit.git
cd habit/bot

# Установить зависимости
pip install -r requirements.txt

# Скачать модель SpaCy для русского языка
python -m spacy download ru_core_news_sm

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env файл

# Запустить бота
python main.py
```

## Настройка

Создайте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your_token_here
DATABASE_URL=sqlite+aiosqlite:///habitmax.db
OCR_SPACE_API_KEY=your_key_here
LOG_LEVEL=INFO
```

## Команды

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/help` | Помощь |
| `/add_habit` | Добавить привычку |
| `/my_habits` | Мои привычки |
| `/stats` | Статистика |
| `/achievements` | Достижения |
| `/add_payment` | Добавить платёж/вклад |
| `/my_finances` | Финансовые напоминания |

## Миграции базы данных

```bash
# Создать миграцию
cd bot
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

## Структура проекта

```
bot/
├── app/
│   ├── handlers/       # Обработчики команд
│   │   ├── common.py   # Общие команды (/start, /help)
│   │   ├── habits.py   # Привычки (оптимизировано)
│   │   └── finance.py  # Финансовые напоминания
│   ├── models/         # SQLAlchemy модели
│   │   ├── base.py     # Базовый класс и сессии
│   │   ├── user.py     # Пользователь
│   │   ├── habit.py    # Привычки и логи
│   │   ├── payment.py  # Платежи и напоминания
│   │   └── achievement.py # Достижения
│   ├── services/       # Бизнес-логика
│   │   ├── finance_reminder.py  # Планировщик финансов
│   │   └── ai_service.py        # AI интеграция
│   └── utils/          # Утилиты
├── config/
│   └── settings.py     # Конфигурация
├── migrations/         # Alembic миграции
├── main.py            # Точка входа
└── requirements.txt   # Зависимости
```

## Оптимизации производительности

### Кеширование привычек
- Предзагрузка активных привычек при старте
- In-memory кеш для быстрого доступа
- Автоматическое обновление кеша

### Быстрая кнопка "Выполнено"
- Мгновенный ответ пользователю (< 50мс)
- Оптимистичное обновление UI
- Тяжёлые операции в фоне
- Индексы БД для частых запросов

## Лицензия

MIT License
